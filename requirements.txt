ffmpeg-python==0.2.0
librosa==0.10.2
torchaudio>=2.2
soundfile==0.12.1
pyloudnorm==0.1.1
numpy>=1.24
pandas>=2.0
datasets>=2.19
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic>=2.7
python-multipart==0.0.9
tqdm>=4.66
jsonlines==4.0.0
khmer-nltk==1.6
regex>=2024.5.15
requests>=2.31

# Optional / heavier deps — install separately if you use them
# torch>=2.2 (match your CUDA version, install from pytorch.org)
# silero-vad (pip install silero-vad, or use torch.hub)
# deepfilternet (pip install deepfilternet)
# fish-speech (git clone into vendor/fish-speech and follow its own setup)

# Optional — scripts/16_auto_eval.py (automated proxy scoring, no torch pin
# since it must match the torch already installed for fish-speech/training)
# transformers>=4.40      (ASR: facebook/mms-1b-all)
# speechbrain>=1.0        (speaker embeddings: ECAPA-TDNN)
# speechmos                (UTMOS naturalness predictor)
